name: onebusaway
description: A Dart client for the OneBusAway REST API.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.4

resolution: workspace

dependencies:
  http: ^1.6.0

dev_dependencies:
  lints: ^6.1.0
  test: ">=1.25.0 <2.0.0"
